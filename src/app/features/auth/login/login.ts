import { Component } from '@angular/core';
import { Button } from '../../../shared/button';

@Component({
   selector: 'app-login',
   imports: [Button],
   templateUrl: './login.html',
   styleUrl: './login.css',
})
export class Login {}
